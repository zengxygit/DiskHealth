1. powershell下先执行：Set-ExecutionPolicy RemoteSigned -Scope Process
2. 然后执行：Convert-QmToTsWithContext_finish.ps1
3. 执行的该脚本的目的是：1）把qm准成ts 2）把ts中main的context的名称改成CDiskHealthMainWin，InfoList的上下文名称也改成CDiskHealthMainWin 3）重新把ts转为qm
4. 转完的ts应该要经过词条审核，比如Temperature很多语言的ts要么没有，要么就是拼写错误
